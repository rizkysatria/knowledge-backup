# SVG Preprocessor Note

For large SVG inputs, use deterministic XML preprocessing outside the Project knowledge bundle to extract structure, IDs, text, relevant attributes, and asset references. Semantic QA interpretation should happen after extraction.
